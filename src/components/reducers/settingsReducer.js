export const ACTIONS = {};

export default function settingsReducer(settings, action) {
    switch (action.type) {
        case ACTIONS:
            return settings;

        default:
            return settings;
    }
}
